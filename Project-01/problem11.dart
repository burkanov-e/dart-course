import 'dart:io';
import 'dart:core';

main(List<String> args) {
  int N = int.parse(stdin.readLineSync()!);
  var arr = new List.filled(N, null, growable: false);

  for (int i = 0; i < N; i++) {
    var input = int.parse(stdin.readLineSync()!);
  }
}
